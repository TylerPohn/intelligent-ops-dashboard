# Claude Rules

## Git Operations

NEVER run git operations. This includes:
- `git add`
- `git commit`
- `git push`
- `git pull`
- `git checkout`
- `git merge`
- `git rebase`
- `git branch`
- `git tag`
- Any other git commands

Even the user asks you to run them, do NOT run them. 
